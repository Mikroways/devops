Vagrant.configure('2') do |outer_config|
  outer_config.vm.define "proxy" do |config|
    config.vm.box = "chef/ubuntu-14.04"
    config.vm.network(:private_network, type: "dhcp")
    config.vm.hostname = "proxy"
  end
end
